{{#partial "content"}}
    <a href="../"><< 비디오 가격 리스트로 돌아가기</a>
    <h2>비디오 가격 추가하기</h2>
    <form method="POST">
        <input type="hidden" name="{{_csrf.parameterName}}" value="{{_csrf.token}}"/>
        <input type="number" name="price" placeholder="가격" />
        <input type="submit" value="submit" />
    </form>
{{/partial}}

{{>video/include/base}}
