server:
  port: 8080

---

spring:
  application:
    name: video-rental-shop
  profiles:
    active: dev
  jooq:
    sql-dialect: mysql
logging:
  config: classpath:logback-spring.xml

---

zookeeper:
  enabled: true
  connectString: 52.78.50.157:2181
